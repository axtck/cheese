import React, { FunctionComponent } from 'react';

interface GameProps { };

const Game: FunctionComponent<GameProps> = () => {
    return (
        <div>
            games
        </div>
    )
}

export default Game;
